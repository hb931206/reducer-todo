export const initialState = {
  todoList: [
    {
      item: "Learn about reducers",
      completed: false,
      id: 1,
    },
  ],
};

export const todoReducer = (state, action) => {
  return state;
};
